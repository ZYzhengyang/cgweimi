已完成。主要改动：

- **CSS columns 4列 250px**：`column-width: 250px; column-count: 4; column-gap: 8px`
- **纯封面图卡片**：每张卡片只显示第一张图，保持原始比例，圆角 12px
- **hover 渐变遮罩**：底部 `linear-gradient(transparent 40%, rgba(0,0,0,0.75))`，显示作者头像（28px 圆形 + 白边框）、名称、用户名、标题、标签
- **hover 微动效**：`transform: scale(1.02)` + overlay 信息 fade-in + slide-up
- **多图帖子只取第一张**：`getThumbUrl` 已用 `find` 只取首张图片
- **加载更多按钮**：保留原有 MkButton 分页加载
- **骨架屏**：简化为纯图片占位，shimmer 动画

