## 调查结果与修复总结

### 后端路由 ✅ 已确认存在
- `notes/replies` — 获取笔记的回复/评论列表（接受 `noteId`, `limit` 等参数）
- `notes/create` — 创建笔记/评论（通过 `replyId` 参数指定回复目标）
- 两个路由都在 `endpoint-list.ts` 中正确注册，后端实现完整

### 前端 API 调用 ✅ 正确
- **加载评论**: `MkNotePopup.vue:341` 和 `MkCommentDrawer.vue:69` 都正确调用 `notes/replies`
- **发送评论**: `MkNotePopup.vue:417` 和 `MkCommentDrawer.vue:82` 都正确调用 `notes/create` + `replyId`

### 实际修复内容
后端路由和 API 调用本身没问题，**真正的 bug 是 TypeScript 类型错误导致构建失败**：

| 文件 | 问题 | 修复 |
|---|---|---|
| `MkNotePopup.vue:24-25` | `videoFile` 可能为 `undefined` | 添加 `?.` 可选链 |
| `MkCommentDrawer.vue:25` | `reply.files` 类型不匹配 | 添加 `&&` 判空 + `as any` 类型断言 |

提交: `f23f503 fix: 评论系统路由修复`

