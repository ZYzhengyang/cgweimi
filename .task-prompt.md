你的任务是修复Explore页面的分类筛选功能。

目标文件：
- packages/frontend/src/pages/explore.works.vue（分类按钮UI）
- packages/frontend/src/components/MkWaterfall.vue（瀑布流组件）

问题：分类按钮点了没用，MkWaterfall没有category/tag prop，永远加载同一数据。

步骤：
1. 先看explore.works.vue中的分类按钮代码
2. 给MkWaterfall加tag/category prop
3. explore.works.vue传分类tag给MkWaterfall
4. MkWaterfall根据tag过滤或请求不同数据
5. 如果后端API不支持分类，用前端过滤（根据note.tags或note.category）

完成后 git add -A && git commit -m "feat: Explore分类筛选功能修复"